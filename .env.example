## Example environment variables for Camellia Shield
# Copy to .env and fill values for your environment

# === REQUIRED ===
# In production: generate with: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=changeme_generate_secure_key_for_production

# === ENVIRONMENT ===
FLASK_ENV=development
FLASK_DEBUG=0
PORT=5000
DESKTOP_MODE=0

# === SESSION/COOKIES ===
SESSION_COOKIE_SECURE=0
SESSION_COOKIE_HTTPONLY=1
SESSION_COOKIE_SAMESITE=Lax

# === RATE LIMITER ===
# Default: file-based storage at /tmp/flask_limiter
# For production: set to redis://localhost:6379 or redis://redis-host:port
# LIMITER_STORAGE=redis://localhost:6379

# === KMS PROVIDER ===
KMS_PROVIDER=file
# KMS_PROVIDER=aws
# AWS_KMS_KEY_ID=arn:aws:kms:region:account:key/id
# AWS_REGION=us-east-1

# === LOGGING ===
# Optional: SIEM endpoint for structured logs (JSON)
# SIEM_ENDPOINT=https://siem.example.com/api/logs
AUDIT_LOG_PATH=audit.log
