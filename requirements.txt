# GateStack — Python dependencies
# Core framework
Flask>=3.1.0,<4.0
Werkzeug>=3.1.0,<4.0
itsdangerous>=2.2.0,<3.0

# Security middleware
flask-talisman>=1.1.0
flask-seasurf>=2.0.0
Flask-Limiter>=3.8.0,<5.0

# Auth & provisioning
argon2-cffi>=25.1.0
PyJWT>=2.9.0,<3.0
pyotp>=2.9.0
qrcode[pil]>=8.0
Pillow>=10.3.0

# Storage / ORM
SQLAlchemy>=2.0.0,<3.0
psycopg[binary]>=3.2.0
requests>=2.32.0
redis>=5.0.0

# Database migrations
alembic>=1.10.0

# Sessions
Flask-Session>=0.4.0

# Application server
gunicorn>=22.0.0

# Dev / test (install via requirements-dev.txt)
# pytest bandit safety flake8 argon2-cffi pyinstaller
