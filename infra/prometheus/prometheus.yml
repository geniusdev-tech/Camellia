global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'gatestack-backend'
    static_configs:
      - targets: ['backend:5000']
    metrics_path: /metrics
    authorization:
      type: Bearer
      credentials: change_me_metrics_token
